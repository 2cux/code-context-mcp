# CodeContext MCP 测试数据说明

`performance/` 长日志和构建输出；`content/` 大代码、大 JSON、RAG chunks、会话历史；`memory/` 100/1000 条 memory seed；`harness-inputs/` flow 输入；`agent-usability/` Agent 工具选择场景；`reports-templates/` 报告模板。

建议复制到项目：`fixtures/mcp-eval/`。

数据为合成数据，不包含真实密钥或真实私有代码。
